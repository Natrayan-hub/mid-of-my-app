// S14 — Health summary (stub).
import React from "react";

import { StubScreen } from "@/src/components/StubScreen";

export default function HealthScreen() {
  return (
    <StubScreen
      title="Health"
      icon="heart"
      description="Readiness, sleep, steps, and trends from your connected sources — plus manual logging."
    />
  );
}
